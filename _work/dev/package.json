{
    "name": "template",
    "version": "1.0.0",
    "description": "App boilerplate",
    "author": "Agora Binaria",
    "license": "ISC",
    "scripts": {
        "prestart": "npm install && cd ../../src/client && npm install && cd ../server && npm install",
        "start": "cd ../../src/server && nodemon ciclismean.js"
    },
    "devDependencies": {
        "nodemon": "^1.4.0"
    }
}
